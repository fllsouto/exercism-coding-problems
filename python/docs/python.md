## Python Walkthrought

### Funções

```python
def <nomedafunção>([<parametro1>,]):
    # Python não precisa de chaves, o escopo é definido pela tabulação
    # Tab de tamanho 4 marca a
    #Preciso colocar explícitamente que a função retorna algo
    return "Hello, World!"

# Exemplo:

def hello_world(name):
  return "Hello, " + name + " !"
```
